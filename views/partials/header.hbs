<nav class="menuBar">
	<div class="menuBar font-effect-outline text-center container">
		<a class="pull-left" href="/">Home</a>
		{{#if user}}
		<p class="text-center">Welcome back {{user.username}}</p>
		<a class="pull-right" href="/logout">Logout</a>
		{{else}}
		<a class="pull-right userLink" href="/signup">Sign Up</a>
		<a class="pull-right userLink" href="/login">Login</a>
		{{/if}}
	</div>
</nav>
{{#if errorMessage}}
<div class="errorMessage">
	<div class="container">{{errorMessage}}</div>
</div>
{{/if}}
